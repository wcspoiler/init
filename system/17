# Command
# Find the command that allows you to connect via ssh on the VM.(In parallel with the graphic session)
# To get your hostname on VM you can use "whoami"
# To get your IP use ifconfig

ssh rel@192.168.56.103